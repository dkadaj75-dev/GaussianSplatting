# CPU-only reconstruction path. pycolmap ships prebuilt wheels, so this needs
# no system COLMAP — unlike worker/, which shells out to the real binaries.
pycolmap>=4.1,<5
numpy>=1.24,<3
scipy>=1.11,<2
pillow>=10,<12
